const ResponsiveLink = ({ title }) => {
    return (
        <div className="inline-block bg-sec font-semibold p-2 shadow-md rounded-md text-white">
            {title}
        </div>
    )
}

export default ResponsiveLink